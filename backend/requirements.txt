# Runtime + test dependencies for the SpatialDebugger backend.
# Nothing here requires an API key.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
httpx>=0.27,<1.0

# Tests
pytest>=8.0,<9.0

# Optional sponsor adapters -- NOT required. Install only if you have keys:
#   openai>=1.40
